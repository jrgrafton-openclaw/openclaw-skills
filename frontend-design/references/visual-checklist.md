# Visual checklist

Before calling the work done, check:
- typography hierarchy and readability
- spacing consistency
- states: default / hover / active / disabled / empty / error where relevant
- responsive behavior at common breakpoints
- contrast and focus visibility
- icon consistency when viewed side-by-side
