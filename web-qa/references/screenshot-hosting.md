# Screenshot handling

Prefer keeping screenshots attached to issues/comments, not committed to feature branches.

If a repo needs a disposable screenshot-hosting branch:
- make the branch clearly temporary
- use traceable filenames
- only force-push if the branch is explicitly disposable
- delete the branch after the issues close

Never commit bug screenshots into the product source tree unless the user explicitly wants fixture assets checked in.
