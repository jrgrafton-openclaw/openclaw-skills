# Deploy rules

- This repo is published through a CI pipeline.
- CI assembles `dist/` and deploys to `gh-pages`.
- Do **not** commit directly to `gh-pages`.
- For cache-sensitive fixes, verify the live site is serving the updated code before declaring success.
